{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Debug CPR/cURL",
            "type": "lldb-dap",
            "request": "launch",
            "program": "${workspaceFolder}/${fileBasenameNoExtension}",
            "args": [],
            "cwd": "${workspaceFolder}",
            "preLaunchTask": "Build with CPR and cURL"
        }
    ]
}
